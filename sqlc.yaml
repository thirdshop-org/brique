version: "2"
sql:
  - engine: "sqlite"
    queries: "core/db/queries"
    schema: "migrations"
    gen:
      go:
        package: "db"
        out: "core/db"
        sql_package: "database/sql"
        emit_json_tags: true
        emit_interface: true
        emit_empty_slices: true
